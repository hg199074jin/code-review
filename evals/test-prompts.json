{
  "skill": "code-review",
  "version": "2.0.0",
  "source": "V2 Review Control Plane eval suite: V1.1 regression coverage plus PR context, R3 security routing, S3 cross-file integration, and bounded review-fix-verify.",
  "darwin_compatible": true,
  "fixtures_root": "evals/fixtures; evals/run.sh builds temporary git repos for deterministic checks.",
  "planted_defects": {
    "A_spec_gap": "invoice SPEC requires total() to skip rows without price; implementation still raises.",
    "A_spec_partial": "invoice SPEC requires apply_discount pct validation; validation is missing.",
    "B_scope_creep": "TAX_RATE and ROUND_DP were added without requirement basis and are unused.",
    "C_regression": "total() now rounds to 2dp, changing prior return semantics without requirement basis.",
    "D_edge_case": "apply_discount accepts pct outside 0-100.",
    "G_weak_test": "discount test covers only the happy path and mirrors current implementation.",
    "discipline_trap": "legacy.py mutable default is pre-existing and must not become a diff finding.",
    "F_command_injection": "runner.py replaced argv-based subprocess execution with os.system(f-string) while job is untrusted.",
    "G_security_test_gap": "test_runner mocks os.system and proves the unsafe implementation rather than malicious-input safety.",
    "C_crossfile_contract": "producer renamed status->state but unchanged consumer still reads status.",
    "G_integration_gap": "test_service checks producer only and never exercises producer->consumer."
  },
  "test_cases": [
    {
      "id": "review-workspace-01",
      "type": "happy_path",
      "prompt": "审查这次修改",
      "repo": "invoice workspace: baseline committed; invoice.py/test_invoice.py modified; SPEC.md untracked",
      "expected": "Resolve deterministic workspace scope; manually inspect excluded test/spec files; catch missing skip branch, missing pct validation, scope creep and weak tests; do not turn legacy.py into a finding; emit A-J coverage and one verdict."
    },
    {
      "id": "review-branch-02",
      "type": "complex",
      "prompt": "帮我看看 feature 分支能不能合到 main",
      "repo": "invoice branch: main=baseline; feature=changed state committed",
      "expected": "Resolve base from the V2 ladder, not feature tracking upstream; review merge-base delta; answer merge-safety with the same material invoice findings."
    },
    {
      "id": "review-nospec-03",
      "type": "degraded",
      "prompt": "审查这次修改，这个项目没什么文档",
      "repo": "invoice workspace with SPEC.md removed",
      "expected": "Mark A as limited because no design basis exists; still review correctness, boundaries, regression, tests and scope; never invent a specification."
    },
    {
      "id": "regress-upstream-trap-04",
      "type": "regression",
      "prompt": "审查 feature-x 分支",
      "repo": "feature-x tracks origin/feature-x; origin/HEAD unavailable; main is the actual merge target",
      "expected": "Never use origin/feature-x as merge base; fall through to main and review the full feature delta."
    },
    {
      "id": "regress-whole-repo-audit-05",
      "type": "regression",
      "prompt": "扫描这个仓库",
      "repo": "committed invoice repo containing legacy.py",
      "expected": "Use local scan preview for enumeration; audit mode may report pre-existing legacy defect; no diff-overlap requirement; disclose audit coverage."
    },
    {
      "id": "regress-old-ocr-compat-06",
      "type": "regression",
      "prompt": "审查这次修改",
      "repo": "invoice workspace with simulated OCR lacking --format",
      "expected": "Retry the identical command without --format only for unknown-flag failure; preserve scope and disclose compatibility fallback."
    },
    {
      "id": "regress-no-test-runner-07",
      "type": "regression",
      "prompt": "审查这次修改",
      "repo": "invoice workspace where native test runner is unavailable and tests are unsafe to direct-import",
      "expected": "Do not blind-import tests; mark E/G limited and never claim tests passed."
    },
    {
      "id": "v2-pr-context-08",
      "type": "v2_context",
      "prompt": "审查 PR #42。PR 描述要求 total 跳过缺 price 的行、apply_discount 越界抛 ValueError；仓库没有 SPEC。",
      "repo": "invoice branch; provider metadata supplied from evals/fixtures/PR42_METADATA.json (base=main, head=feature, title/body); no SPEC.md. Harness feeds the metadata as environment context — never fetched from a live provider.",
      "expected": "Use PR base/head for scope and PR body as intent context; catch the two requirement gaps while making clear that PR text is context, not infallible authority. PR metadata comes from the fixture file (OR-003), so the scenario runs offline."
    },
    {
      "id": "v2-r3-security-09",
      "type": "v2_high_risk",
      "prompt": "审查这次修改",
      "repo": "high-risk runner fixture from HIGH_RISK_SPEC.md: safe argv subprocess changed to os.system(f-string)",
      "expected": "Classify R3; require an independent Security/Reliability lens or sequential equivalent; report command injection as P0/P1 with E1 code-path proof; report the implementation-shaped test gap; do not need an external scanner to find it."
    },
    {
      "id": "v2-s3-integration-10",
      "type": "v2_large_change",
      "prompt": "审查这次大型修改",
      "repo": "run.sh generated >20-file change; producer status->state changed, unchanged consumer still expects status; producer-only test",
      "expected": "Classify S3; review in batches and run a final cross-batch integration pass; catch producer/consumer contract drift even though the broken consumer line is unchanged; cite both sides; identify missing integration test."
    },
    {
      "id": "v2-review-fix-verify-11",
      "type": "v2_fix_loop",
      "prompt": "审查并修复这次修改，然后确认问题都解决了",
      "repo": "high-risk runner fixture",
      "expected": "Freeze initial finding IDs before fixes; authoring agent fixes authorized P0/P1; run minimal relevant tests; VERIFY marks original IDs FIXED/OPEN/REGRESSED and any new issues NEW; stop after at most two cycles unless user explicitly requests more."
    }
  ]
}