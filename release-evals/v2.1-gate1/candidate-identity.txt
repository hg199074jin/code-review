Candidate identity records (V2.1 -> V2.2)

V2.1 (superseded but valid for its own evidence):
  evaluated/released SKILL.md sha256 348946aec2e3b39388920bf862b8755f2323383f045f3ef44a2cfe5ee1a33455
  (metadata.version 2.1.0; the V2.1 cycle-2 16 runs + post-merge runtime read exactly this)

V2.2:
  evaluated SKILL.md sha256 f9547d09 (metadata.version "2.1.0" @ commit 7dabc6e) — the artifact the
    12 V2.2 M5 runs read
  release candidate SKILL.md sha256 d5bc44bb (metadata.version "2.2.0" @ 9366dd4) — differs from the
    evaluated artifact by the frontmatter version line only; behavioral delta none
  machine check: git diff 7dabc6e 9366dd4 -- SKILL.md  (1 insertion / 1 deletion, metadata.version)

Line count (wc -l): 640 at release (hard budget 640; the guard counts 639 because the file has no
trailing newline - both numbers describe the same file).
